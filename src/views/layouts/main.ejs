<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- 基本SEOメタ -->
    <meta name="description"
        content="<%= locals.seo?.description || locals.description || 'ボーイスカウト多治見第一団の公式ウェブサイト。活動内容、入団案内など。' %>">
    <meta name="keywords" content="<%= locals.seo?.keywords || 'ボーイスカウト,多治見,岐阜県,青少年育成,野外活動' %>">
    <meta name="robots" content="<%= locals.seo?.robots || 'index, follow' %>">
    <meta name="author" content="ボーイスカウト多治見第一団">

    <!-- Canonical URL -->
    <% if (locals.seo?.canonical) { %>
        <link rel="canonical" href="<%= locals.seo.canonical %>">
        <% } %>

            <!-- Favicon -->
            <link rel="icon" type="image/x-icon" href="/favicon.ico">
            <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">

            <!-- Open Graph Protocol -->
            <meta property="og:type" content="<%= locals.seo?.ogType || 'website' %>">
            <meta property="og:title" content="<%= locals.seo?.ogTitle || locals.title || 'ボーイスカウト多治見第一団' %>">
            <meta property="og:description" content="<%= locals.seo?.description || locals.description || '' %>">
            <meta property="og:url" content="<%= locals.seo?.canonical || '' %>">
            <meta property="og:site_name" content="<%= locals.seo?.siteName || 'ボーイスカウト多治見第一団' %>">
            <meta property="og:image" content="<%= locals.seo?.ogImage || '' %>">
            <meta property="og:locale" content="ja_JP">

            <!-- Twitter Card -->
            <meta name="twitter:card" content="<%= locals.seo?.twitterCard || 'summary_large_image' %>">
            <meta name="twitter:title" content="<%= locals.seo?.ogTitle || locals.title || '' %>">
            <meta name="twitter:description" content="<%= locals.seo?.description || locals.description || '' %>">
            <meta name="twitter:image" content="<%= locals.seo?.ogImage || '' %>">

            <title>
                <%= locals.seo?.title || locals.title || 'ボーイスカウト多治見第一団' %>
            </title>

            <!-- 構造化データ (JSON-LD) -->
            <% if (locals.seo?.jsonLD) { %>
                <script type="application/ld+json">
    <%- locals.seo.jsonLD %>
    </script>
                <% } %>

                    <!-- Preconnect for performance -->
                    <link rel="preconnect" href="https://fonts.googleapis.com">
                    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
                    <link
                        href="https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c:wght@400;700;800&family=Noto+Sans+JP:wght@400;500;700&display=swap"
                        rel="stylesheet">
                    <link rel="stylesheet" href="/styles/common-styles.css">

                    <script defer src="/common-scripts.js"></script>
                    <script defer src="https://unpkg.com/alpinejs@3.14.3/dist/cdn.min.js"></script>

                    <% if (locals.pageScripts && Array.isArray(locals.pageScripts)) { %>
                        <% locals.pageScripts.forEach(script=> { %>
                            <script defer src="<%= script %>"></script>
                            <% }) %>
                                <% } %>
</head>

<body class="bg-gray-100 text-gray-800 antialiased" x-data>

    <%- include('../partials/header') %>

        <main>
            <%- body %>
        </main>

        <%- include('../partials/footer') %>

</body>

</html>